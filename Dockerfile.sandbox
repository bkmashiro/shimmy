FROM golang:1.21-alpine

RUN apk add --no-cache gcc musl-dev python3

WORKDIR /app

# Copy sandbox module
COPY sandbox/ ./sandbox/

# Build
RUN cd sandbox && go build -v .

# Copy test scripts
COPY sandbox-poc/malicious_tests.py ./
COPY sandbox-poc/lambda_probe.py ./

CMD ["sh"]
